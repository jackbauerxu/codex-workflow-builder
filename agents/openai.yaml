interface:
  display_name: "Codex Workflow Builder"
  short_description: "把任务和视频生产做成可复用系统"
  default_prompt: "Use $codex-workflow-builder to turn my repeated task or video pipeline into a reusable production system with a lane, style preset, script, shots, references, quality gates, and a concrete project workflow."
policy:
  allow_implicit_invocation: true
