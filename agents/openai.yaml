interface:
  display_name: "Codex Workflow Builder"
  short_description: "把任务和视频生产做成工作流"
  default_prompt: "Use $codex-workflow-builder to turn my repeated task, editing routine, or HyperFrames video pipeline into a concrete Codex project workflow."
policy:
  allow_implicit_invocation: true
